<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base theme with action bar -->
    <style name="Theme.ZachenAway" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>
        <item name="colorPrimary">#838387</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>

    <!-- Theme without action bar -->
    <style name="Theme.ZachenAway.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>-->
        <item name="windowNoTitle">true</item>
    </style>

</resources>
